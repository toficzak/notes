application.properties:

spring.data.mongodb.database=<database>
spring.data.mongodb.port=27017 
spring.data.mongodb.host=localhost

[AI] check if port 27017 can be ommited
[AI] check if localhost can be ommited


[AI] launch authentication and use those below #no idea how, will pick up
#spring.data.mongodb.authentication-database=admin
#spring.data.mongodb.username=root
#spring.data.mongodb.password=root

### Entites

actually does not need @Document, but it's probably good practice to mark Documents. 
for sure when document name in database should be different than JPA document class name, it can be specified as @Document("name")

[AI] What are those autogenerated IDs? Looks like hex.

### Repo
extends MongoRepository<Entity, Id> <-- pretty normal, huh?

### Scratchbook:
curl 127.0.0.1:8080/api/books | jq
curl 127.0.0.1:8080/api/books/646fc7538f840808826dac7e
curl -X POST localhost:8080/api/books --data "{\"name\":\"test_book4\"}" -H "Content-Type: application/json"



